@model List<Model.EF.Sprint>
<!--Vùng hiển thị list các sprint trong 1 phase-->
<div class="col-lg-12" style="padding-left: 0px;">
    <div class="panel panel-default">
        <div class="panel-heading" style="text-align: left; font-size: 25px">
            List Sprint in Phase
        </div>
        <div class="panel-body" style="padding-bottom: 15px;">
            <div class="panel panel-default">
                <table class="table table-striped table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>Sprint</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr class="gradeA odd" role="row">
                                <td class="sorting_1">
                                    <a href="/Sprint/DetailSprint?idSprint=@item.idSprint">@item.sprintName</a>
                                </td>
                                <td class="sorting_1">
                                    <span>@item.status</span>
                                </td>
                            </tr>
                        }
                    </tbody>     
                </table>
            </div>
            @Html.ActionLink("Create Sprint", "CreateSprint", "Sprint", null, new { @class = "btn btn-success" })
        </div>
    </div>
</div>
